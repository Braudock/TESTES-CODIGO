proc sort data=seus_dados out=seus_dados_sorted;
  by sua_coluna;
run;

proc sql;
  create table dados_duplicados as
  select *
  from seus_dados_sorted
  group by sua_coluna
  having count(*) > 1;
quit;
