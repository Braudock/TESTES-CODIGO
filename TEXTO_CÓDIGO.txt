data nova_tabela;
    set tabela_exemplo;
    select;
        when (condicao1) nova_coluna = 'Resultado1';
        when (condicao2) nova_coluna = 'Resultado2';
        otherwise nova_coluna = 'OutroResultado';
    end;
run;
