PROC SQL;
SELECT
    NOME_ESPEC,
    FUNC_ESPEC,
    RACF_ESPEC,
    SEGMENTO,
    COUNT(RACF_ESPEC) AS QTD_RACF
FROM
    VALIDA_SEGMENTO AS V
WHERE
    QTD_SEGTO > 1
GROUP BY
    NOME_ESPEC,
    FUNC_ESPEC,
    RACF_ESPEC,
    SEGMENTO
;
RUN;
