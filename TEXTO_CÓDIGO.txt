PROC SQL;
SELECT
    A.SEGMENTO,
    A.NOME_ESPEC,
    A.FUNC_ESPEC,
    A.RACF_ESPEC,
    COUNT(A.SEGMENTO) AS QTD_SEGTO
FROM VALIDA_SEGMENTO AS A
WHERE QTD_SEGTO > 1
GROUP BY 1, 2, 3, 4
;RUN;
