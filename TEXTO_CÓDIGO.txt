PROC SQL;
SELECT
    A.SEGMENTO,
    A.NOME_ESPEC,
    A.FUNC_ESPEC,
    A.RACF_ESPEC,
    COUNT(A.SEGMENTO) AS QTD_SEGTO
FROM
    VALIDA_SEGMENTO AS A
GROUP BY
    A.SEGMENTO,
    A.NOME_ESPEC,
    A.FUNC_ESPEC,
    A.RACF_ESPEC
HAVING
    COUNT(A.SEGMENTO) > 1
;
RUN;
