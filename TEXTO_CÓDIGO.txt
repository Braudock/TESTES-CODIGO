data seu_dataset_formatado;
    set seu_dataset_original;

    /* Formatar a Coluna_4 para ter 4 dígitos, preenchendo com zeros à esquerda */
    Coluna_4 = put(Coluna_4, z4.);

    /* Formatar a Coluna_7 para ter 7 dígitos, preenchendo com zeros à esquerda */
    Coluna_7 = put(Coluna_7, z7.);
run;
