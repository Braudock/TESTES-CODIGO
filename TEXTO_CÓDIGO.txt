PROC SQL;
    SELECT
        coluna,
        Contagem
    FROM
        (SELECT
             coluna,
             COUNT(*) AS Contagem
         FROM
             tabela
         GROUP BY
             coluna
        ) AS subconsulta
    WHERE Contagem > N; /* Substitua N pelo número que deseja filtrar */
QUIT;
