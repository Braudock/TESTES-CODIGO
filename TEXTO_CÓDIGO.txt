import pandas as pd
import os

# Lista dos caminhos dos arquivos Excel
caminhos = [
    "caminho/para/o/arquivo1.xlsx",
    "caminho/para/o/arquivo2.xlsx",
    # Adicione mais caminhos conforme necessário
]

# Dicionário para armazenar o nome do arquivo e a quantidade de linhas
contagem_linhas = {}

for caminho in caminhos:
    try:
        # Lendo o arquivo Excel
        df = pd.read_excel(caminho)
        # Extraindo apenas o nome do arquivo do caminho completo
        nome_arquivo = os.path.basename(caminho)
        # Contando as linhas e armazenando no dicionário
        contagem_linhas[nome_arquivo] = len(df)
    except Exception as e:
        # Em caso de erro, armazena a mensagem de erro
        nome_arquivo = os.path.basename(caminho)
        contagem_linhas[nome_arquivo] = str(e)

# Exibindo os resultados
for nome, linhas in contagem_linhas.items():
    print(f"Arquivo: {nome}, Linhas: {linhas}")
