import requests
import warnings

# Suprimir os avisos SSL
warnings.filterwarnings("ignore", message="Unverified HTTPS request")

proxy = {
    

todos = requests.get("https://jsonplaceholder.typicode.com/todos", proxies=proxy, verify=False)