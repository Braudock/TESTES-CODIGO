data tabela_original;
    set tabela_original;
    /* Converte a própria coluna para o tipo data */
    coluna_caractere = input(coluna_caractere, yymmdd10.);
    /* Define o formato de exibição como data */
    format coluna_caractere date9.;
run;