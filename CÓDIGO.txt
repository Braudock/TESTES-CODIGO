data tabela_original;
    set tabela_original;
    /* Converte a coluna de caractere para data em uma coluna temporária */
    coluna_temp = input(coluna_caractere, yymmdd10.);
    format coluna_temp date9.;
    /* Exclui a coluna original e renomeia a coluna temporária */
    drop coluna_caractere;
    rename coluna_temp = coluna_caractere;
run;