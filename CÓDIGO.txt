from requests_ntlm import HttpNtlmAuth
import requests

url = "http://site-corporativo.com/imagem"
username = "DOMINIO\\seu_usuario"
password = "sua_senha"

response = requests.get(url, auth=HttpNtlmAuth(username, password))