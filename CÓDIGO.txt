data nova_tabela;
    set tabela_original;
    /* Converte a coluna de caractere para data */
    nova_data = input(coluna_caractere, yymmdd10.);
    /* Define o formato de exibição como data */
    format nova_data date9.;
run;